name: code-scan

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  scan-code:
    uses: aiblockofficial/workflows/.github/workflows/scan-code.yml@main
  scan-image:
    uses: aiblockofficial/workflows/.github/workflows/scan-image.yml@main
    secrets: inherit
    with:
      IMAGE: ${{ vars.REGISTRY }}/aiblockofficial/node-miner:latest
